// import { LambdaTransformer, Router } from '../..';

describe('lambda', () => {
    // let router: Router;
    // let lambdaTransformer: LambdaTransformer;

    // beforeEach(() => {
    //     router = new Router();
    //     lambdaTransformer = new LambdaTransformer(router);
    // });

    it('', () => {
        expect(true).toBe(true);
        // expect(httpTransformer.requestListener()).toBe(1);
    });
});
